import styles from './Footer.module.css'

function Footer() {
    return(
        <footer className={styles.footer}>
            Site desenvolvido com React &copy
        </footer>
    )
}

export default Footer